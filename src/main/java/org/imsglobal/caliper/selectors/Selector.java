package org.imsglobal.caliper.selectors;

/**
 * The Selector interface marks an object type that can be used to indicate the boundaries of a targeted selection.
 * The interface allows Caliper to catch errors at compile time for instances of the marked class rather than
 * at runtime if a marker annotation was defined instead.
 */
public interface Selector {

}
