package org.imsglobal.caliper.entities.lis;

/**
 * The course interface marks an object type that represents a LIS Course.
 * The interface allows Caliper to catch errors at compile time for instances of the marked
 * class rather than at runtime if a marker annotation was defined instead.
 */
public interface Course extends org.imsglobal.caliper.entities.w3c.Organization {

}