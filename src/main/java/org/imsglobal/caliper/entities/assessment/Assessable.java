package org.imsglobal.caliper.entities.assessment;

/**
 * The assessable interface marks an object type that can be used to assess, appraise or evaluate.  The interface allows
 * Caliper to catch errors at compile time for instances of the marked class rather than at runtime if a marker
 * annotation was defined instead.
 */
public interface Assessable {

}
